# Changelog

## [5.9.19] - 2026-04-26
### Added
- **Настройка «Жалобы: магазины-исключения»** в табе Настройки — `#complaintExcludeInput` (textarea) + `#disableOzonBlacklist` (checkbox). Пользовательский список добавляется К дефолтному Ozon-blacklist, либо чекбокс полностью отключает дефолт. Сохраняются в `chrome.storage.local.complaintExcludeSellers` / `ozonBlacklistDisabled`
- **Подсказка `data-hint-host="complaint_exclusions"`** в Настройках — стандартная скрываемая подсказка через ✕, восстанавливается через «Показать подсказки» (ключ добавлен в `SETTINGS_HINT_KEYS`)
### Changed
- **Удалён фиксированный зелёный блок** `.complaint-blacklist-note` под `#complaintSkuInput` в табе Жалобы — заменён скрываемой подсказкой в Настройках
- **`isBlacklistedComplaintSeller()` использует runtime-конфиг** — `complaintExcludeList` (из storage) + `DEFAULT_OZON_BLACKLIST` если `ozonBlacklistDisabled === false`
- **Лог при «В жалобы»** — теперь говорит конкретно что пропущено (Ozon-магазины + пользовательские исключения, или только пользовательские если дефолт отключен)
### Removed
- **CSS `.complaint-blacklist-note`** — заменён на `.checkbox-row` для нового чекбокса

## [5.9.18] - 2026-04-26
### Added
- **Watchdog для бота жалоб** (`background/service-worker.js`) — `chrome.alarms.create('supportWatchdog', { periodInMinutes: 1 })` будит SW раз в минуту, проверяет `Date.now() - supportState.lastActivityTs`. На 3+ мин idle — пишет в лог `[WATCHDOG] Нет активности N мин` + DOM-снимок (`phase`, `chatMsgCount`, `hasInput`, `hasFileInput`, `hasSendButton`, последние сообщения бота/юзера). На 6+ мин — ставит паузу и шлёт `supportNeedAction` с просьбой проверить чат и прислать лог. `_bumpActivity()` вызывается в `supportLog` — каждый лог продлевает ttl
- **Хардкод-blacklist для жалоб** (`popup/popup.js` `COMPLAINT_SELLER_BLACKLIST`) — `['ozon', 'озон', 'интернет решения', 'internet solutions']`. При «В жалобы» SKU магазинов с этими подстроками в `sellerName` пропускаются, в лог пишется счётчик и список первых 3 имён. Защита от инцидента когда бот пожаловался на товар Ozon Беларусь. Инфо-блок `.complaint-blacklist-note` в HTML под `#complaintSkuInput`
- **Подробная диагностика прикреплений** (`background/service-worker.js` фаза `waiting_attachment`):
  - Перед каждым файлом — лог размера в КБ + MIME-типа (`(${fileSizeKb} КБ, тип ${file.type || '?'}, baseline ${baselineCount})`)
  - Время прикрепления в мс (`за ${attachMs}мс`)
  - При провале верификации — финальный DOM-снимок: `msgs ${baselineCount}→${finalCount}, last user: «...», last bot: «...»`
  - После провала всех файлов — `[ATTACH-DIAG]` с состоянием input/sendBtn/viewport
- **Счётчик подряд-провалов прикреплений** — `supportState.consecutiveAttachFails`. После 1 провала — подсказка о возможных причинах. После 5 — `⚠ N SKU подряд... Возможно Ozon заблокировал чат от спама ИЛИ изменился интерфейс. Скопируйте лог и пришлите в t.me/firadex`. НЕ блокирует работу — бот продолжает (это не критическая ошибка)
### Changed
- **Имитация человека сохранена** — все паузы между файлами/SKU остались, watchdog их не задевает (3 мин idle ≈ 6× больше типичной паузы)
- **Watchdog НЕ останавливает бота на единичных задержках** — только после 6 минут idle (нормальная подача жалобы редко занимает >2 мин)

## [5.9.17] - 2026-04-24
### Fixed
- **Критический баг форматтера ввода лицензии** — `popup/popup.js` при каждом `input` стрипал все не-alphanumeric символы и принудительно пересобирал ключ в формат 3-5-5-5, ломая любые ключи с блоками иной длины (например `OZG-TEST1-LIFE-00001` → `OZG-TEST1-LIFE0-0001` → сервер возвращал `License not found`). Фикс: форматтер теперь non-destructive — только `toUpperCase()` и удаление символов кроме `[A-Z0-9-]`; дефисы пользователя сохраняются. `maxlength` на `#licenseCodeInput` поднят с 21 до 32
- **Расхождение error-контракта клиент ↔ сервер** — `codefic /api/license` шлёт человеческие строки (`License not found`, `Activation limit reached`, `Not activated on this device`, ...), а расширение искало snake_case (`invalid_key`, `max_activations`, ...). Последствия: клиент всегда видел общую «Ошибка активации»; `backgroundVerify` никогда не чистил отозванные ключи, а через 7 дней grace молча флипал в FREE. Фикс: `normalizeLicenseError()` в `background/service-worker.js` маппит обе формы (подстрочный матч + exact snake_case) в общий код
- **Fingerprint дрейф после реинсталла не восстанавливался** — если клиент распаковал новый ZIP в другую папку → extension ID → новый fingerprint → сервер отвечал `Not activated on this device`. Старый код не знал этой ветки и лицензия через 7 дней grace превращалась в FREE. Фикс: `backgroundVerify` на ошибке `not_activated_here` молча вызывает `activate` со старым ключом и текущим fp — если квота позволяет, лицензия восстанавливается без участия пользователя. Если квота исчерпана (`max_activations`), показывает конкретное сообщение
### Added
- **Расширенная диагностика лицензии в Настройках** — новые блоки `#licenseErrorHelp` (под полем ввода) и `#licenseDiagBox` (под активным ключом). Словарь `LICENSE_HELP` в `popup/popup.js` для каждого кода ошибки (`invalid_key`, `revoked`, `expired`, `max_activations`, `not_activated_here`, `verification_needed`, `network_error`, `rate_limited`) выводит: заголовок, сообщение, 2-3 конкретных шага для клиента, ссылки на личный кабинет codefic.ru и поддержку t.me/firadex. CSS-стили `.license-error-help`, `.license-diag` в `popup/popup.css`
- **`licenseLastError` в storage** — при каждой неудачной активации/верификации расширение сохраняет `{code, message, at}` в `chrome.storage.local`. `getLicenseStatus` пробрасывает это в `status.lastError` для popup. Очищается при успешной активации или deactivate
### Changed
- **`activateLicense` теперь возвращает `{success, code, error}`** — поле `code` с нормализованным кодом ошибки, чтобы popup мог показать детальную подсказку без повторного маппинга. Обратно совместимо (старый код `resp.error` остался)

## [5.9.16] - 2026-04-24
### Fixed
- **Выпадающее меню `#complaintType` выходило за пределы popup** — опции «Использование моего контента (BETA)» / «Использование моего бренда (BETA)» были длиннее чем `0.5fr` grid-колонка. Фикс: (1) `complaint-settings-grid` колонки изменены на `0.45fr / 0.55fr` (тип жалобы шире), (2) `.input-select` внутри grid получил `min-width: 0`, `width: 100%`, `text-overflow: ellipsis` — длинный текст обрезается многоточием вместо overflow, (3) тексты опций укорочены с `title`-атрибутом для полного описания при hover: «Плагиат моих карточек», «Мой контент (BETA)», «Мой бренд (BETA)» (`popup/popup.css`, `popup/popup.html`)
### Added
- **Кнопка «Читать об обновлении» в баннере новой версии** — рядом с «Скачать» добавлена ссылка на `https://codefic.ru/#changelog`. Ведёт на блок с описанием обновлений простым языком на сайте (`popup/popup.html`: `#updateChangelogLink`, `popup/popup.css`: `.update-banner-sep`)
- **`CHANGELOG-USER.md`** — второй файл для обычных пользователей и клиентов. Описывает релизы без технических терминов. Загружается на сайт codefic.ru через админку и рендерится в блоке `#changelog` (под блоком скачивания расширения)
- **`site-changelog-update-guide.md`** — инструкция для ИИ/разработчика сайта: как оформить блок Обзор обновлений на codefic.ru с учётом стиля сайта, примеры разметки, структура данных
### Changed
- **`.claude/CLAUDE.md`** — добавлено правило: при каждом релизе дублировать запись в `CHANGELOG-USER.md` на простом языке

## [5.9.15] - 2026-04-24
### Added (3 типа жалоб вместо 2, новые BETA-пути навигации в чате Ozon)
- **Три варианта типа жалобы в UI** — вместо `seller/brand` теперь:
  - `plagiat_legacy` (по умолчанию, стабильный) — «Плагиат моих карточек» (старый путь Личный кабинет → Кабинет бренда → Жалоба на товар → Плагиат)
  - `content_beta` — «Использование моего контента (BETA)» (Товары и Цены → Контроль качества → Нарушение правил площадки → Использование моих фото, видео, текста)
  - `brand_beta` — «Использование моего бренда (BETA)» (тот же путь, финальная кнопка «Использование моего бренда»)
- **Path-resolver `resolveNavButton(phase, path)`** — для каждой навигационной фазы возвращает набор паттернов кнопки в зависимости от выбранного пути. Унифицирует 3 ветки без дублирования кода (`background/service-worker.js`)
- **`handleNavPhase(tabId, phase, state)`** — общий обработчик навигационных фаз с встроенными защитами: проверяет что нужная кнопка реально видна в меню перед кликом, считает retry-попытки (3 max), логгирует каждое действие с префиксом `[BETA]` для новых путей (`pathLog()`)
- **BETA autostop** — `checkBetaAutostop()` + `supportState.consecutiveFailed` + `betaAutostopLimit=5`. При 5 SKU подряд в failed на BETA-путях бот ставит паузу с сообщением «Возможно путь в чате Ozon изменился» и предлагает подтвердить продолжение или остановиться. Использует существующий limit-gate UI (`supportLimitReached`-message)
- **Предупреждение BETA под селектором в popup** — `#betaWarning` показывается когда выбран `content_beta` или `brand_beta`: «Режим BETA — новый путь... протестируйте на 2-3 SKU... при 5 ошибках автостоп» (`popup/popup.html`, `popup/popup.js`: `updateBetaWarning`)
- **Детекторы новых кнопок в `content/support-automation.js`**:
  - `complaint_type` — добавлена детекция «Нарушение правил площадки другим продавцом» (признак BETA-меню: соседние кнопки «Скрытие товаров», «Документы качества»)
  - `complaint_detail` — добавлена детекция «Использование моего бренда»
  - `category_selection` — «Контроль качества» (подменю «Товары и Цены»)
  - `direction_selection` — «Товары и Цены»
- **`knownPatterns` в `has_buttons`-fallback** пополнен: «товары и цены», «контроль качества», «использование моего бренда», «нарушение правил площадки» — если detection фазы не сработал, fallback найдёт знакомую кнопку
- **DRY run** — теперь выводит навигационный путь согласно выбранному complaintType (не только legacy как раньше)
### Changed
- **Миграция старых значений `seller`/`brand` → `plagiat_legacy`** при восстановлении `lastComplaintSession` в popup и при приёме `msg.complaintType` в background — backward compatibility для пользователей которые обновляются с v5.9.14
- **`complaintType` по умолчанию** в `supportState` изменён с `'seller'` на `'plagiat_legacy'`
- **BETA-защита сбрасывает `navClickRetries`** при переходе к следующему SKU — не копятся между items
- **user-guide.md и help-модалка** — обновлены с таблицей 3 типов, описанием BETA-режимов и рекомендациями по тестированию
### Fixed
- **Устранена дублированная логика** в 5 навигационных фазах (direction/category/complaint_type/subtype/detail) — теперь единая точка входа `handleNavPhase()`. Это снимает риск разъезжания retry-логики между фазами

## [5.9.14] - 2026-04-23
### Added
- **Общая кнопка «💡 Показать подсказки» в Настройках** — одна кнопка на весь таб Настройки, возвращает все скрытые подсказки (exclusions, delay, cascade, cascade_limit, cascade_consec, sku_files). Появляется если скрыта хотя бы одна из них. Стиль `.btn-show-hints` переиспользован (`popup/popup.html:tabSettings`, `popup/popup.js`: `btnShowSettingsHints`, `restoreHintGroup`, `updateShowHintsButtons`)
- **Подсказка к блоку доказательств в Жалобах** — новый hint-banner с `data-hint-host="evidence"`: «Общий пул доказательств прикрепится ко всем SKU, для которых нет персональных файлов в Настройках. Если у SKU есть персональные — бот использует именно их. Если нет ни тех ни других — failed». Скрывается крестиком как остальные подсказки в Жалобах, возвращается кнопкой «Показать подсказки» в табе Жалобы. Добавлена в группу `COMPLAINT_HINT_KEYS`
### Changed
- **Текст зоны загрузки файлов** в Жалобах — вместо «📎 Доказательства — перетащите или нажмите» теперь «📎 Общие доказательства (fallback для SKU без персональных файлов) — перетащите или нажмите», явно отражает приоритет per-SKU файлов
- **Accept в общем пуле файлов** — добавлен `video/*` (был только image+pdf+doc). Видео до 200 MB уезжают в IndexedDB как в per-SKU блоке
- **Логика кнопок «Показать подсказки»** унифицирована через `restoreHintGroup(keys, btn)` — работает для обеих групп (Жалобы и Настройки). Группы `COMPLAINT_HINT_KEYS`/`SETTINGS_HINT_KEYS` — явные константы

## [5.9.13] - 2026-04-23
### Fixed (CRITICAL — пауза не прерывала выполнение)
- **Пауза (⏸) не останавливала бот между действиями** — когда пользователь жал ⏸ во время `await delay(betweenDelay)` (7-15с антибот-пауза между жалобами), флаг `isPaused` выставлялся, но код после await продолжал линейно: клик «Пожаловаться на другой» → ждать 4с → ждать 5с. Loop останавливался только на следующей итерации, но к моменту бот уже делал действия. В логах было видно: `[22:55:15] Пауза → [22:55:17] Нажимаю «Пожаловаться на другой товар»`
- Введена функция `interruptibleDelay(ms)` — разбивает длинную паузу на шаги по 250ms, между шагами проверяет `isRunning`/`isPaused`. Возвращает `false` если прервана. Вызывающий код возвращает `'stop'` при прерывании, loop корректно завершается
- Применено в 5 местах:
  - `ready_for_next` блок — все 3 long-delay (betweenDelay 7-15с, обновление страницы 4с, доп.ожидание 5с, retry кнопки 5с)
  - `checkConsecutiveEscalations` — длинная антибот-пауза 20-35с при 0-лимите (брендовый режим)
  - `chat_escalated` retry — 10с ожидание в том же чате
  - `waiting_attachment` — цикл верификации файла (15-30с по 1с) теперь проверяет isPaused после каждой секунды
- Дополнительно: непосредственно перед кликом «Пожаловаться на другой» добавлена прямая проверка `if (!isRunning || isPaused) return 'stop'` — защита от race между `supportPause`-message и уже начавшимся шагом
### Changed
- **Подсказки в Настройках сделаны скрываемыми крестиком** — вместо кнопок управления в отдельном блоке, описания к секциям «Исключения», «Задержка», «Защита от каскада», «Доказательства для SKU» теперь оформлены как `.hint-banner.hint-info` с ✕ (как в табе Жалобы). Ключи в `dismissedHints`: `exclusions`, `delay`, `cascade`, `cascade_limit`, `cascade_consec`, `sku_files`. Переиспользован существующий механизм скрытия (`popup/popup.html`, `popup/popup.js`: расширен `collectHintElements` через `data-hint-host`-атрибуты)
- **Убрано**: блок «Подсказки интерфейса» с двумя кнопками из v5.9.12 (правильное понимание запроса пользователя — унифицированный механизм через крестик, без отдельного контрола)
### Added
- **`.hint-banner.hint-compact` CSS** — компактный вариант подсказки для плотных экранов Настроек (меньше отступы/шрифт)

## [5.9.12] - 2026-04-23
### Added
- **Кнопка «📎 К доказательствам» в строке каждого родительского SKU в сканере** — клик переносит SKU в Настройки → «Доказательства для отдельных SKU», предзаполняет поле, прокручивает и подсвечивает секцию. Если к SKU уже привязаны файлы — кнопка зелёная «✓ Доказательства» с другим tooltip. Бейдж автообновляется через `chrome.storage.onChanged` когда пользователь добавляет/удаляет файлы в Настройках (`popup/popup.js`: `skusWithEvidenceCache`, `refreshEvidenceBadges`, `refreshSkusWithEvidenceCache`; `popup/popup.html` в `.my-sku-row`; `popup/popup.css`: `.btn-evidence-sku`, `.highlight-settings`, `.highlight-bundle` с keyframes-анимацией)
- **Блок «Подсказки интерфейса» в Настройках** — две кнопки «Скрыть все подсказки» / «Показать все подсказки». Раньше управление подсказками было только через крестик на каждом баннере в вкладке «Жалобы» (ключ `dismissedHints` в storage, баннеры VPN и инструкции) (`popup/popup.html`, `popup/popup.js`: обработчики `btnHideAllHints`/`btnShowAllHints`)
### Changed
- **user-guide.md обновлён** — добавлены разделы «Подсказки интерфейса», «Жалобы: защита от каскада обращений», «Жалобы: доказательства для отдельных SKU» с кнопкой быстрого добавления, подробное описание статусов очереди (done/escalated/failed/pending/processing), блок «Проблемные SKU» и гейт лимита
- **Help-модалка в расширении** — расширена секция «🔍 Сканирование» (4-й шаг про кнопку «К доказательствам»), секция «📜 История» (упомянута сохраняемая parent→competitor связь), секция «⚙️ Настройки» (подсказки, каскад-защита, per-SKU доказательства), секция «📨 Жалобы PRO» (два режима доказательств, блок проблемных SKU, гейт лимита)

## [5.9.11] - 2026-04-23
### Fixed (CRITICAL — ложные failed при реально отправленных файлах)
- **Файл-сообщения не детектировались в `readChatHistory`** — фильтр `text.length < 3` отбрасывал user-file-сообщения у которых `getMessageText` возвращает пусто или короткую строку (только превью-иконка без видимого имени). Теперь: если признак файла (svg-иконка / класс `om_15` / ссылка на cdn/pdf/jpg/mp4/docx / `\d+ Kb/Mb` в тексте) — сообщение попадает в историю независимо от длины текста (`content/support-automation.js:readChatHistory`)
- **Расширенная детекция isFile** — добавлены 2 новых сигнала: `a[download]`/`a[href*="cdn"]`/`a[href*=".pdf/.jpg/.png/.mp4/.mov/.webm/.docx"]` и regex на размер файла в тексте (`\d+ Кб/Мб`). Без этого новые превью от Ozon с линком вместо svg-иконки не распознавались как файл
- **Верификация прикрепления по ответу бота** — раньше требовалось одновременно `chatMsgCount > baseline` И `lastUserMsg === '[FILE]'`. Если Ozon отвечал «Скрыли товар…» быстрее чем мы замечали `[FILE]` — верификация падала. Теперь успех если ЛЮБОЕ из: (а) `lastUserMsg === '[FILE]'`, (б) `lastBotMsg` содержит маркер принятия (`скрыли товар`, `нарушение подтвердилось`, `рассмотрим`, `получили ваш`, `направил/и`, `пожаловаться на другой`, SKU артикула), (в) `chatMsgCount` вырос на 2+ (файл + ответ одновременно)
- **Базовое окно верификации увеличено** с 10с до 15с (крупные файлы по-прежнему 30с)
- **Реальный эффект** — клиент на скрине: PDF 809KB отправлен, бот Ozon ответил «Скрыли товар 4057476401» ✓✓, но расширение ложно помечало 3 SKU подряд как failed. После фикса такие SKU корректно помечаются `done`

## [5.9.10] - 2026-04-23
### Added (крупный пакет доработок жалоб после live-фидбека клиента на пакете 339 жалоб)
- **Защита от каскада обращений** — бот считает `newChatsOpened` за сессию и `consecutiveEscalations` подряд. При достижении лимитов (10 новых чатов / 5 эскалаций подряд по умолчанию) ставит паузу и показывает popup-gate с кнопками «Продолжить» (разрешает ещё порцию) / «Остановить». Настраивается в вкладке «Настройки → Жалобы: защита от каскада». 0 эскалаций = «брендовый режим» без остановок с увеличенной паузой (`background/service-worker.js`: `canOpenNewChat`, `checkConsecutiveEscalations`, `registerNewChatOpened`, новые поля `supportState.limits/newChatsOpened/consecutiveEscalations/limitGate*`; `popup/popup.html`, `popup/popup.css`, `popup/popup.js`)
- **Верификация handoff перед новым обращением** — перед навигацией на новый чат проверяет `lastBotMsg` на маркеры «направил/направили/коллегам/рассмотрит/оператор/создайте новое обращение». Если обращение не подтверждено как переданное оператору — помечает SKU как `failed` «Не удалось подтвердить передачу оператору» и НЕ создаёт новый чат (защита от пустых тикетов, `verifyItemHandedOff` в `service-worker.js`)
- **Прикрепление нескольких файлов** — раньше использовался только `supportState.files[0]` (первый файл), остальные игнорировались. Теперь бот итерирует по всем файлам, каждый отдельно прикрепляет и отправляет, с верификацией `lastUserMsg === '[FILE]'` в чате (до 30с ожидания для крупных файлов/видео) (`background/service-worker.js` блок `waiting_attachment`)
- **Per-SKU доказательства** — новый блок в «Настройки»: добавить свой SKU + файлы (видео, сертификат). Бот при обработке жалобы на конкурента-нарушителя смотрит `parentSku` и прикрепляет файлы привязанные к этому родителю. Если SKU конкурента найден из нескольких родителей (Q6) — объединяются файлы со всех. Fallback на общий пул когда для SKU ничего не привязано. Если ни общего, ни per-SKU нет — `failed` (`popup/popup.html`, `popup/popup.js`: UI `skuFiles*`, `background/service-worker.js`: `pickFilesForItem`, `supportState.skuFiles/parentMap`)
- **Блок «Проблемные SKU» в popup** — две категории: `🛎 Передано оператору` (escalated, ожидают ручной обработки) и `✗ Ошибки` (failed из-за разных причин). Для каждой категории: «📋 Копировать», «↻ В очередь» (перенести SKU обратно во ввод для повтора), «📥 XLSX» (скачать CSV с причинами и временем), «✕» (очистить). Persist в `chrome.storage.local.complaintProblems` — переживает перезапуск popup (`popup/popup.html`, `popup/popup.css`, `popup/popup.js`: `complaintProblems`/`notifyProblem`/`problemsDownloadXlsx`)
- **Поддержка видео-доказательств и больших файлов** — файлы >5MB автоматически идут в IndexedDB (лимит 200MB на файл), мелкие остаются в `chrome.storage.local` для совместимости. В UI файл получает бейдж `IDB` и размер в MB. Permission `unlimitedStorage` в manifest. Одноразовая миграция старых `complaintFilesData` → `complaintFilesMeta/complaintFilesBlobs` при первом запуске popup (`manifest.json`, `popup/popup.js`: IndexedDB хелперы `ozg*`, accept `video/*` в input, `persistComplaintFiles`)
- **Новый статус `escalated`** в очереди SKU (отличается от `failed` ✗ и от `done` ✓) — отображается значком 🛎 и оранжевым цветом; отдельно трекается в storage `complaintProgress` для skip-логики при повторном запуске
- **Карта parent→competitor** — при нажатии «📨 В жалобы» (на вкладке Сканирование и в Истории) сохраняется `complaintParentMap: {competitorSku: [parentSku, ...]}`. Используется для выбора per-SKU файлов в боте. Поддерживается накопление по нескольким сессиям сканирования
### Changed
- **Ozon-лимит файлов поднят с 10MB до 200MB** с обязательным предупреждением пользователя когда файл >10MB уезжает в IndexedDB
- **Тексты эскалации** — вместо `failed` / «Чат эскалирован оператору — ожидает ручной обработки» теперь отдельный статус `escalated` с текстом «Передано оператору — ожидает ручной обработки»
### Fixed
- **Прикрепление «не работало» при пакетном импорте** — корень: `supportState.files[0]` использовался вместо всего списка. Теперь все файлы прикрепляются по очереди. Клиент с 339 SKU и 5 файлами теперь получит каждый файл на каждом SKU
- **`waiting_attachment` застревало** без явного failed — теперь если за `maxWait` (10с/30с для крупных) `lastUserMsg !== '[FILE]'` и ни один файл не прошёл, SKU помечается `failed` «Ни один файл не удалось прикрепить», добавляется в блок проблемных, бот идёт дальше

## [5.9.9] - 2026-04-19
### Changed
- **Жалобы: эскалированный чат больше не отменяется** — раньше при `chat_escalated` бот кликал «Отменить обращение», что закрывало чат и терялась возможность оператора рассмотреть жалобу вручную. Теперь бот пишет оператору «Пожалуйста рассмотрите жалобу», затем навигирует на страницу чатов и открывает новое обращение для следующего SKU. Эскалированное обращение остаётся в очереди оператора. Статус SKU: `done` (если жалоба была подтверждена до эскалации) или `failed` с пояснением «ожидает ручной обработки» (`background/service-worker.js`)
### Fixed
- **Ссылка на обновление расширения больше не ведёт на 404** — popup использует `https://codefic.ru/#install` как безопасный fallback если сервер не прислал `download_url` ИЛИ вернул прямой `.zip` URL (часто отдаёт 404 если файл не загружен). Новая функция `resolveDownloadUrl()` валидирует URL и отсеивает zip-ссылки. Install-страница содержит инструкцию + актуальную кнопку скачивания — лучше UX чем прямой zip (`popup/popup.js`)

## [5.9.8] - 2026-04-19
### Changed (BREAKING — монетизация)
- **Весь функционал сбора SKU теперь бесплатен** — множественная проверка, пакетный XLSX-импорт, все экспорты. Раньше требовалась PRO-подписка для 2+ SKU и XLSX. PRO-подписка остаётся только для автоматической подачи жалоб через чат поддержки OZON (`background/service-worker.js`: снят paywall в `startScan`; `popup/popup.js`: убрана проверка `isProLicense` в `btnStart` и `handleBatchFile`, убран класс `paywall-locked` с `batchDrop`)
- **Обновлены тексты** — убраны упоминания PRO для скана во всех местах: help-модалка (`popup/popup.html`), матрица FREE/PRO (`project.md`, `user-guide.md`), описание лицензии в Настройках
- **Матрица FREE/PRO** — единственная PRO-функция теперь: «Автоматическая подача жалоб через чат поддержки»

## [5.9.7] - 2026-04-19
### Fixed
- **Жалобы: ложное «Зацикливание in_progress»** — лимит `maxPhaseRepeats = 4` применялся ко всем фазам. Для `in_progress` (ожидание ответа бота Ozon) это давало таймаут ~20с, а Ozon на медленном интернете отвечает 30-60с. Теперь для `in_progress` отдельный лимит 12 (~60с ожидания). Остальные фазы — 4 как раньше (`background/service-worker.js`)
- **Жалобы: ложное «кнопка цикла не работает»** — 3 попыток клика «Пожаловаться на другой» (21с) было мало на медленном Win11/Ozon. Теперь 5 попыток с увеличенным ожиданием (5с вместо 4с). После 5 неудач вместо `failed` → **принудительная навигация на новый чат** (как при `chat_escalated`) → бот сам откроет новое обращение для этого SKU. Только если навигация упадёт — `failed` (`background/service-worker.js`)
### Added
- **Проверка новой версии в popup** — при открытии popup запрос к `https://codefic.ru/api/extension-version` (кэш 6ч в `chrome.storage.local.extensionVersionCache`). Если сервер вернул `version` новее текущей:
  - Бейдж версии в хедере подсвечивается оранжевым градиентом + pulse анимация
  - Клик на бейдж открывает `download_url`
  - Под хедером баннер «🎉 Доступна новая версия vX.Y.Z — Скачать»
  - Кнопка ✕ скрывает баннер (запоминается `dismissedUpdateVersion`, показывается снова когда сервер отдаст ещё более новую)
  (`popup/popup.html`, `popup/popup.css`, `popup/popup.js`)
- **extension-version-api-guide.md** — инструкция для админки сайта: endpoint `/api/extension-version`, таблица `extension_versions`, UI загрузки новой версии, процесс релиза

## [5.9.6] - 2026-04-19
### Fixed
- **Жалобы: SKU с эскалированным чатом теперь пропускается, а не повторяется** — в v5.9.4 `chat_escalated` возвращал SKU в `pending` для повтора в новом чате. Но повторная обработка того же SKU в новом чате снова вызывала эскалацию (Ozon требует новое обращение, и эскалация не зависит от номера чата). Теперь SKU с неподтверждённой жалобой при эскалации помечается `failed` с `error: 'Чат эскалирован оператору — SKU пропущен'`, `currentIndex++` → бот переходит к следующему SKU в новом чате. Подтверждённый до эскалации SKU (есть «скрыли товар» в ответе) по-прежнему помечается `done` (`background/service-worker.js`)

## [5.9.5] - 2026-04-19
### Fixed (CRITICAL)
- **Жалобы: бот писал SKU в закрытый чат после «Отменить обращение»** — после клика «Отменить обращение» чат закрывается, но поле ввода остаётся. Бот видел `input_ready` и начинал писать следующий SKU в завершённое обращение (никуда не отправлялось, но время терялось и логика терялась). Фикс: после успешного клика «Отменить обращение» ВСЕГДА принудительная навигация на `/app/messenger/?group=support_v2` + инжект скриптов. Фаза становится `no_chat`/`faq_page` → бот корректно открывает новый чат через «Помощь» (`background/service-worker.js`)
- **Жалобы: popup терял логи и очередь когда был закрыт** — `supportState.logs`/`queue` жили только в памяти service worker и в `lastComplaintSession.logs` (DOM popup). Если popup был закрыт во время работы бота, при переоткрытии он видел только то что успел записать сам. Теперь:
  1. `persistActiveSupportSession()` — debounced (1с) запись полной сессии в `chrome.storage.local.activeSupportSession` при каждом `supportLog` и `supportProgress`/`supportComplete`/`supportNeedAction`
  2. `supportGetStatus` возвращает состояние **всегда** — если в памяти нет (SW перезапустился), читает из `activeSupportSession`. Добавлено поле `source: 'memory' | 'storage' | 'none'`
  3. Popup при открытии восстанавливает очередь SKU с бейджами done/failed/pending и полный лог сессии — даже если бот уже завершился
  4. `supportStart` очищает старый `activeSupportSession`; `btnComplaintClear` тоже
  (`background/service-worker.js`, `popup/popup.js`)

## [5.9.4] - 2026-04-18
### Added
- **Жалобы: детекция эскалации чата (`chat_escalated`)** — новая фаза в `detectPhase()`. Триггеры: текст бота «направил ваше обращение коллегам», «создайте новое обращение», «нажмите Отменить обращение», или кнопка «Отменить обращение» в quick-replies. Обработчик в `supportProcessStep`:
  - Если текущий SKU не подтверждён (нет «Скрыли товар» + SKU в ответе бота) → возвращает его в pending для повтора
  - Кликает «Отменить обращение» (fallback: навигация на `/app/messenger/?group=support_v2`)
  - Сбрасывает `_staleWaitCount`, `lastPhase`, `phaseRepeatCount`
  - Следующая итерация попадает в фазу `no_chat`/`faq_page` → бот открывает новый чат и повторяет навигацию
  (`content/support-automation.js`, `background/service-worker.js`)
- **Жалобы: persistent progress** — `saveSupportSession()` теперь сохраняет `complaintProgress.processedSkus` в `chrome.storage.local` после каждого обработанного SKU. При следующем «Начать»:
  - Popup проверяет пересечение входящих SKU с `processedSkus`
  - Показывает `confirm()`: ОК → пропустить обработанные, Отмена → начать заново (`resetProgress: true`)
  - Service worker помечает обработанные SKU как `done/failed/skipped` в `queue`, стартует с первого `pending`
  - Если все SKU обработаны → статус `all_done` с подсказкой очистить прогресс
  - `btnComplaintClear` очищает `complaintProgress` вместе с `lastComplaintSession`
  - Новые actions: `supportResetProgress`, `supportGetProgress`
  (`background/service-worker.js`, `popup/popup.js`)
- **Скоринг кнопки «Отменить обращение»** — добавлен в `findQuickReplyButtons` score +10 для надёжного поиска (`content/support-automation.js`)

## [5.9.3] - 2026-04-12
### Fixed (CRITICAL — CPU freeze)
- **Жалобы: рекурсия логирования → CPU freeze** — `supportLog()` обновлял плавающую панель через `sendToSupport(updatePanel)`. При ошибке связи `sendToSupport` логировал «Нет связи — инжектирую…» через `supportLog()`, который снова вызывал `sendToSupport`. `injectSupportScripts` тоже логировал ошибки через `supportLog`. Каждый лог порождал 2 новых → экспоненциальный каскад промисов в одной секунде → CPU freeze, помогало только убить браузер. Теперь:
  1. `supportLog` → `updatePanel` идёт через прямой `chrome.tabs.sendMessage` без авто-инжекта и без рекурсивного логирования
  2. `injectSupportScripts` и `sendToSupport` пишут технические ошибки в `console.log`, не в `supportLog`
  3. Throttle `updatePanel`: не чаще 1 раза в секунду (`PANEL_UPDATE_INTERVAL_MS = 1000`)
  (`background/service-worker.js`)
### Added
- **Circuit breaker для injection** — если `chrome.scripting.executeScript` падает >= 5 раз за 10 секунд, автоматизация останавливается с понятным сообщением «Закройте старые вкладки seller.ozon.ru, откройте новую и нажмите Начать» (`background/service-worker.js`)
- **Pre-flight проверка вкладок при `supportStart`** — `preflightSellerTabs()` детектирует:
  - 0 вкладок → создаёт новую автоматически
  - 2+ вкладки → ошибка `multiple_tabs` с просьбой оставить только одну
  - 1 вкладка но injection падает → ошибка `stale_tab`: «старая вкладка от предыдущей сессии расширения, откройте новую»
  Pre-flight ошибки выводятся в popup жёлтым блоком предупреждения (`background/service-worker.js`, `popup/popup.js`)

## [5.9.2] - 2026-04-05
### Fixed
- **Быстрый скан: товары не в наличии** — OZON возвращает страницу категории вместо товара (44-50 виджетов, title "Витамины - купить на OZON"). Добавлена `isProductPage()`: проверяет наличие `productHeading`, упоминание SKU в widgetStates, маркер категории в title. При обнаружении категории — пропуск с ошибкой «Товар не найден или не в наличии» (`background/service-worker.js`)
- **Триал: злоупотребление при переустановке** — fingerprint сохранялся только в `chrome.storage.sync/local` (очищается при удалении расширения). Теперь дополнительно сохраняется в `localStorage` сайта ozon.ru (ключ `__ozg_fp`, MAIN world) — переживает переустановку. При установке расширение восстанавливает fingerprint оттуда (`background/service-worker.js`)
### Added
- **trial-hardening-guide.md** — инструкция для серверной защиты триала: привязка к IP + UserAgent, rate limit (3 триала/IP за 90 дней), таблица `trials`, логика activate/validate, управление в админке

## [5.9.1] - 2026-04-04
### Changed
- **Жалобы: полный редизайн UI** — компактные подсказки (VPN + инструкция) вместо громоздкого info-блока, настройки в 2-колоночной сетке, кнопки управления в одну строку с корзиной-иконкой, файлы компактнее (`popup/popup.html`, `popup/popup.css`)
- **Подсказки: persistent dismiss** — закрытие подсказок сохраняется в `chrome.storage.local` (переживает перезапуск расширения, сбрасывается при переустановке). Кнопка «💡 Показать подсказки» возвращает все скрытые (`popup/popup.js`)
- **Кнопка «Визуальный» → «🐢 Медленный»** — tooltip: «безопаснее для сессии, не нагружает антибот» (`popup/popup.html`)
### Added
- **tariff-migration-guide.md** — инструкция для ИИ по миграции тарифа «Навсегда» → «Годовая подписка»: серверный API, ЮКасса, сайт, email-шаблоны, админка, backward compatibility

## [5.9.0] - 2026-04-04
### Added
- **Быстрый режим сканирования (⚡)** — прямые API-запросы к Ozon без открытия окна браузера. В 5-10 раз быстрее визуального режима. Использует скрытый таб (или существующую вкладку www.ozon.ru) для выполнения authenticated fetch к `/api/entrypoint-api.bx` и `/api/composer-api.bx` (`background/service-worker.js`)
- **`fetchProductDataDirect()`** — запрос данных товара напрямую по SKU через API без навигации по страницам. Пробует product page API → search API → поиск товара в результатах → запрос найденного товара (`background/service-worker.js`)
- **`fetchSellersListDirect()`** — запрос списка продавцов через modal API без навигации (`background/service-worker.js`)
- **`scanSkuFast()`** — оркестратор быстрого сканирования одного SKU: прямой API → парсинг продавцов → modal → фильтрация (`background/service-worker.js`)
- **`parseSellersFromModalData()`** — общий парсер продавцов из modal data, используется и быстрым и визуальным режимом (`background/service-worker.js`)
- **Переключатель режима сканирования** — UI-кнопки «⚡ Быстрый» / «🖥 Визуальный» в табе Сканирование. Режим сохраняется в `chrome.storage.local` (`popup/popup.html`, `popup/popup.js`, `popup/popup.css`)
### Changed
- **Автоматический fallback** — если быстрый режим не получает данные 3 раза подряд, автоматически переключается на визуальный (открывает окно) для оставшихся SKU (`background/service-worker.js`)
- **Оптимизированные задержки** — в быстром режиме задержка между SKU 600мс (vs 2000мс в визуальном), антибот-паузы 3-8 сек каждые 20 SKU (vs 10-20 сек) (`background/service-worker.js`)
- **Рефакторинг `fetchSellersList()`** — дублированный парсинг продавцов вынесен в общий `parseSellersFromModalData()` (`background/service-worker.js`)

## [5.8.2] - 2026-04-04
### Changed
- **Жалобы: авто-открытие вкладки** — `findSellerTab()` автоматически создаёт новую вкладку `seller.ozon.ru/app/messenger/?group=support_v2` если ни одной вкладки seller не найдено (`background/service-worker.js`)
- **Жалобы: проверка авторизации** — `ensureSellerChatPage()` определяет редирект на логин (`/signin`, `/login`, `passport.ozon.ru`) и выводит предупреждение войти в аккаунт (`background/service-worker.js`)
- **Жалобы: восстановление закрытой вкладки** — если вкладка закрыта во время работы, `supportProcessStep()` и `ensureSellerChatPage()` автоматически ищут/создают новую вкладку (`background/service-worker.js`)
- **Жалобы: защита от ухода со страницы** — `supportProcessStep()` проверяет что URL остаётся на `seller.ozon.ru`, при уходе — автовозврат на чат поддержки (`background/service-worker.js`)
- **Жалобы: информативные сообщения** — все ошибки связи/авторизации теперь содержат конкретные инструкции для пользователя (что открыть, куда перейти)
### Added
- **Жалобы: обход FAQ-виджета (двухшаговый)** — новая фаза `faq_page` в `detectPhase()`. На messenger-странице без открытого чата бот: 1) находит и кликает плавающую кнопку «Помощь» (SVG-иконка, триггер tippy), 2) в открывшемся tippy-popup кликает «Не нашли ответ на свой вопрос?» → открывается реальный чат с ботом. Исправлено определение `no_chat` — теперь проверяется `findChatContainer()` напрямую, а не через fallback `findQuickReplyButtons` который ловил nav-табы (Аналитика, Покупатели...) как "кнопки чата" (`content/support-automation.js`, `background/service-worker.js`)
- **VPN-предупреждение** — в разделе Жалобы блок-рекомендация отключить VPN, закрывается на крестик (запоминается в sessionStorage) (`popup/popup.html`, `popup/popup.js`, `popup/popup.css`)
- **Отображение остатка триала** — в Настройках при активном триале показывается блок «Пробный период: осталось N дн.» (`popup/popup.html`, `popup/popup.js`, `popup/popup.css`)

## [5.8.1] - 2026-04-03
### Fixed (CRITICAL)
- **Жалобы: чат покупателя вместо поддержки** — `findSellerTab()` выбирал любую вкладку seller.ozon.ru, включая `group=customers_v2`. Теперь строгий приоритет: `group=support_v2` → messenger без group= → любая вкладка (с последующим redirect)
- **Жалобы: `ensureSellerChatPage` не перенаправлял** — URL с `/app/messenger?group=customers_v2` проходил проверку `includes('/app/messenger')`. Теперь проверяется именно `group=support_v2`
- **Жалобы: параллельная вкладка с покупателем** — добавлена проверка URL перед каждым шагом `supportProcessStep()`: если URL содержит `group=customers`, бот автоматически перенаправляет на `support_v2`
- **CPU freeze / зависание браузера** — рекурсивные вызовы `supportProcessStep()` (~30 мест) заменены на итеративный цикл `supportProcessLoop()` с `MAX_STEP_ITERATIONS=200`, `await delay(50)` между шагами для yield к event loop, аварийная остановка при превышении лимита
- **Зацикливание `in_progress`** — бот отправлял артикул в чат покупателя (не в поддержку), получал `in_progress` бесконечно. Исправлено валидацией URL + strict tab selection

## [5.8.0] - 2026-04-02
### Added
- **Пробный период PRO (3 дня)** — кнопка «Попробовать PRO бесплатно — 3 дня» в Настройках. Серверная привязка к fingerprint: один триал на устройство навсегда
  - `trial_activate` — активация через `POST /api/license { action: "trial_activate" }`
  - `trial_validate` — проверка при каждом открытии расширения (если нет ключа)
  - Кеширование статуса в `chrome.storage.local` (trialStatus, trialExpiresAt, trialCheckedAt)
  - 4 состояния: FREE / TRIAL / PRO / TRIAL_EXPIRED
  - Бейдж TRIAL (оранжевый) в header, обратный отсчёт дней
  - После истечения: блок «Пробный период закончился» + CTA «Купить PRO»

## [5.7.4] - 2026-04-01
### Added
- **Кнопка инструкции «?»** в header — модальное окно с подробной инструкцией по всему функционалу: сканирование, XLSX-импорт, экспорт, история, настройки, жалобы, FAQ. Ссылки на codefic.ru и seller.ozon.ru

### Changed
- **Header** — PRO/FREE бейдж сдвинут к логотипу, кнопка «?» и версия объединены в `header-right`

## [5.7.3] - 2026-04-01
### Added
- **Запоминание вкладки** — расширение сохраняет последнюю открытую вкладку и восстанавливает её при следующем открытии (`chrome.storage.local` → `lastActiveTab`)
- **История → «Жалобы»** — кнопка «📨 Жалобы» на каждой карточке истории (появляется если в сессии есть SKU конкурентов). Отправляет SKU в таб Жалобы и переключает на него
- **Умная проверка запуска жалоб** — перед стартом проверяется наличие открытой вкладки seller.ozon.ru. При отсутствии — жёлтый блок ошибки с кликабельной ссылкой и инструкцией

### Changed
- **Таб Сканирование** — кнопки экспорта (Скопировать / SKU / Excel / В жалобы / Очистить) перенесены наверх над списком результатов, закреплены с `position: sticky`
- **Режим жалоб по умолчанию** — «Авто» вместо «Тестовый» при очистке сессии
- **seller.ozon.ru** — превращён в кликабельную ссылку в инструкции таба Жалобы

## [5.7.2] - 2026-03-27
### Added
- **Импорт CSV в жалобы** — кнопка «Импорт из Excel» в разделе Жалобы: загружает CSV-файл из истории сканирования, извлекает SKU конкурентов (колонка 3) и добавляет в textarea артикулов

### Fixed
- **Фильтр XLSX: клик открывал файловый диалог** — клики на чекбоксы/кнопки batch-фильтра пробрасывались на зону загрузки файла. Добавлена фильтрация `event.target` в `batchDrop` click handler
- **Плавающая панель не появлялась** — `scan-panel.js` инжектился только при создании окна, но `chrome.tabs.update` уничтожает content scripts. Добавлена `injectScanPanel()` после каждой навигации
- **Кнопки Пауза/Стоп неактивны при работе бота** — при переоткрытии popup состояние сканирования терялось. Добавлен `getScanStatus` в service-worker + восстановление UI при открытии popup
- **SKU mismatch без кнопок** — результаты с ошибкой «SKU не совпал» не имели кнопки копирования и ссылки на OZON. Добавлены `my-sku-row` элементы в блок ошибок

### Changed
- **Дизайн batch-фильтра** — карточка с рамкой и фоном, увеличенные чекбоксы (16px, accent-color), hover на строках, кнопка «Применить» на всю ширину

## [5.7.1] - 2026-03-25
### Added
- **Плавающая панель сканирования** — `content/scan-panel.js`: прогресс-панель на ozon.ru во время сканирования с кнопками Пауза/Стоп/Закрыть, drag-перетаскивание, лог в реальном времени
- **Фильтр статусов XLSX** — после загрузки файла «Цены товаров» отображаются чекбоксы по статусам (Продается, Готов к продаже, Не продается) с кнопками «Все» / «Применить»
- **Копирование SKU + ссылка на OZON** — в результатах сканирования рядом с каждым SKU кнопка копирования и ссылка «открыть на OZON»

### Fixed
- **SKU mismatch при поиске** — `findProductOnPage()` теперь приоритизирует ссылки, содержащие SKU. После навигации проверяет URL на совпадение с SKU, при несовпадении — пропускает товар
- **CSS стили** — добавлены `.my-sku-row`, `.my-sku-link`, `.btn-copy-my-sku`, `.batch-filter` стили в `popup/popup.css`

## [5.7.0] - 2026-03-25
### Added
- **Фильтр исключений** — в Настройках новый блок «Исключения при сборе»: список имён продавцов (по одному на строку), которые автоматически исключаются из результатов сканирования. Частичное совпадение. Сохраняется в `chrome.storage.local` → `excludeSellers[]`
- **Кнопка «Купить PRO»** — в header и в секции лицензии. Ссылка на `codefic.ru/#pricing`, скрывается при активной PRO-лицензии
- **`user-guide.md`** — полная инструкция пользователя: установка, все 4 таба, пошаговые сценарии, технические детали, графические рекомендации для сайта

### Changed
- **Настройки** — удалены: «Как это работает», «Статус подключения», «Диагностика». Оставлены: Лицензия PRO, Исключения при сборе, Задержка между запросами
- **Фильтр продавцов** — вместо одного поля «Мой магазин» (ownSellerName) теперь массив `excludeSellers[]` с множественными исключениями
- **Поле «Мой магазин»** удалено из таба Сканирование (перенесено в Настройки → Исключения)

### Fixed
- **API URL mismatch** — расширение вызывало `POST /api/license/{action}`, сервер ожидает `POST /api/license` с `{action}` в body

## [5.6.1] - 2026-03-25
### Fixed (CRITICAL)
- **API URL mismatch** — расширение вызывало `POST /api/license/{action}` (action в URL path), сервер ожидает `POST /api/license` с `{action, ...}` в body. Все вызовы лицензии исправлены на единый endpoint

### Added
- **Кнопка «Купить PRO»** — в header (рядом с FREE-бейджем) и в секции лицензии в настройках. Ссылка на `codefic.ru/#pricing`. Скрывается при активной PRO-лицензии

### Changed
- **Чувствительные данные** — убраны комментарии и упоминания в CHANGELOG, deploy-guide.md
- **deploy-guide.md** — обновлены curl-примеры на единый API endpoint

## [5.6.0] - 2026-03-24
### Fixed (CRITICAL)
- **SKU пропуск / зацикливание на `ready_for_next`** — при перезапуске бота на существующем чате или после обработки предыдущего SKU, новый SKU видит `ready_for_next` и должен кликнуть «Пожаловаться на другой товар». Раньше бот либо помечал SKU как «обработанный» без действий, либо ждал смену фазы бесконечно. Fix: smart handler сразу кликает кнопку цикла (до 3 попыток), верифицирует смену фазы, и только при неудаче — ошибка. Стандартная защита от зацикливания пропускает этот случай (smart handler управляет сам)
- **`getMessageText()` стриппил текст бота** — `cursor:pointer` проверка удаляла текст из крупных контейнеров сообщений (вся строка бота вычиталась). Fix: стриппятся только мелкие элементы (height < 50px, width < 500px, без блочных дочерних, < 8 слов) — реальные quick-reply кнопки
- **`input_ready` fallback слишком агрессивный** — срабатывал через 2 повтора (6с), теперь через 3 повтора (9с) + расширены паттерны запроса артикула (`номер товара`, `укажите товар`, `sku`, `отправьте артикул`)

### Added
- **Smart Self-Correction System** — интеллектуальная система самокоррекции бота жалоб:
  - **Stale state detection** — распознаёт устаревшую фазу от предыдущего SKU, ждёт обновления страницы (до 5 попыток × 3с), затем пробует кликнуть кнопку цикла
  - **SKU verification** — перед пометкой «обработано» проверяет, что бот упомянул текущий SKU / содержит ключевые фразы результата
  - **Phase transition verification** — после клика «Пожаловаться на другой» проверяет, что фаза действительно сменилась с `ready_for_next`, добавляет доп. ожидание если нет
  - **Retry кнопки цикла** — при ошибке «Кнопка цикла не найдена» ждёт 5с и пробует повторно
  - **Подробный `[SMART]` лог** — все действия самокоррекции логируются с префиксом `[SMART]` для диагностики

### Fixed
- **API URL mismatch** — расширение вызывало `POST /api/license/activate` (action в URL path), а сервер ожидает `POST /api/license` с `{action, key, fingerprint}` в body. Все вызовы теперь идут на единый endpoint
- **VERIFY_INTERVAL** — исправлен интервал фоновой проверки: 24ч (было 12ч) для соответствия серверному API

### Changed
- **`getMessageText()`** — полностью переписана логика фильтрации: вместо удаления ВСЕХ cursor:pointer элементов, фильтрует по размеру, структуре и длине текста
- **`input_ready` → 3 повтора** — увеличен порог fallback с 2 до 3, добавлен вывод текста бота в лог для диагностики
- **Комментарии** — убраны упоминания чувствительной информации из кода и CHANGELOG

## [5.5.0] - 2026-03-20
### Fixed (CRITICAL)
- **`readChatHistory()` читал кнопки как текст бота** — `.textContent` на `[is-mine=""]` включал quick-reply кнопки («Назад», «Пожаловаться на другой товар»). После «Пожаловаться на другой товар» `lastBotMsg` = «Назад» вместо «Пришлите скопированное значение артикула». Fix: `getMessageText()` вычитает из textContent текст элементов с `cursor:pointer` и таймстемпы
- **`input_ready` зацикливание** — если `lastBotMsg` не содержал ключевых слов артикула (из-за бага выше), бот ждал бесконечно. Fix: fallback после 2 повторов — отправляет артикул без проверки текста бота
- **Расширены паттерны запроса артикула** — добавлены `'пришлите одно'`, `'значение артикула'` в проверку `isArticleRequest`
- **Двойная отправка артикула** — `simulateRealClick()` делал `dispatchEvent('click')` + `element.click()` = два click-события → React отправлял сообщение дважды. Fix: убран дублирующий `.click()`
- **Файл не отправлялся (waiting_attachment loop)** — `attachFile()` ждал 3с вместо реальной загрузки файла. Fix: ожидание превью до 10с (поиск имени файла / SVG-иконки в области ввода). Retry: если файл не появился в чате (lastUserMsg ≠ FILE), step откатывается для повторной попытки

### Changed
- **LICENSE_API** — Supabase Edge Function заменён на собственный сервер `codefic.ru/api/license`
- **Маскировка кода** — popup показывает `OZG-*****-*****-*****` вместо полного ключа
- **Режим жалоб** — «Автоматический» по умолчанию (первый в списке), «Тестовый» второй
- **Инструкции в popup** — актуализированы: добавлены пакетный импорт и жалобы в настройках, обновлено описание режимов жалоб

### Added
- **`deploy-guide.md`** — руководство по деплою, управлению лицензиями, устранению проблем

### Removed
- **Supabase** — полностью удалена зависимость от Supabase Edge Functions

## [5.4.0] - 2026-03-20
### Changed (BREAKING)
- **Лицензия — серверная валидация** — полная замена оффлайн-валидации на серверную через API. Поддержка месячных и вечных ключей. `background/service-worker.js`: `getDeviceFingerprint()`, `licenseApiCall()`, `getLicenseStatus()`, `backgroundVerify()`, `activateLicense()`, `deactivateLicense()`, `scheduleLicenseCheck()`
- **Device fingerprint** — UUID в `chrome.storage.sync` (переживает переустановку, привязка к аккаунту браузера), fallback в `chrome.storage.local`
- **Периодическая проверка** — `chrome.alarms` каждые 12ч + при старте service worker. Offline grace period 7 дней

### Added
- **`manifest.json`** — пермишен `alarms` для периодической проверки лицензии
- **Popup UI для типа лицензии** — бейдж «Вечная» / «Месячная», счётчик оставшихся дней, предупреждение при ≤3 дней. `popup/popup.html`: `licenseTypeInfo`, `licenseTypeBadge`, `licenseDaysLeft`
- **Статусы истёкшей лицензии** — popup показывает «Подписка истекла» или «Требуется проверка» вместо просто «FREE»
- **CSS стили** — `.license-type-badge` (lifetime/monthly), `.license-days-left` (+ `.expiring`), `.license-type-info`

### Removed
- **Оффлайн DJB2 валидация** — полностью удалена из service-worker.js

## [5.3.0] - 2026-03-20
### Fixed (CRITICAL)
- **Quick-reply кнопки чата НЕ НАХОДИЛИСЬ** — два бага: (1) `findAllClickableWithText()` искал только `button, [role="button"], a[href]`, но OZON использует `div`/`span` с `cursor: pointer`; (2) фильтр `el.closest('[is-mine]')` убивал ВСЕ кнопки внутри сообщений бота — нужно пропускать только `[is-mine="mine"]`
- **Единое дерево навигации** — в OZON чате НЕТ отдельного пути для seller-жалоб. Оба типа идут: Кабинет бренда → Жалоба на товар/продавца → Плагиат → Использование моих фото. Убрано разделение паттернов по `complaintType` — теперь единые паттерны для всех фаз (`category_selection`, `complaint_subtype`, `complaint_detail`)
- **Навигационные фазы hard-stop** — если кнопка не найдена, бот останавливался навсегда. Теперь ждёт 3с и retry
- **Дублирование артикула** — `input_ready` отправлял артикул до того как бот его запросил. Теперь проверяет `lastBotMsg` на ключевые слова запроса артикула перед отправкой. Также `item.step` проверяет `article_sent`/`file_sent`/`completed`

### Added
- **Имитация человека** — `simulateRealClick`: mousemove к элементу (2-4 точки), случайный offset внутри элемента (не центр), рандомные паузы между событиями. `setInputValue`: посимвольный ввод для contenteditable (30-100мс/символ). Навигационные фазы: рандомная пауза 0.6-2.2с перед кликом, 2.5-5с после. Антибот-пауза между SKU: 7-15с

### Added
- **Плавающая панель прогресса на seller.ozon.ru** — видна даже когда popup закрыт. Показывает: прогресс-бар, логи (последние 50), статус. Сворачивается. Обновляется через `supportLog()` → `updatePanel` action в content script
- **`findChatContainer()`** — находит scrollable-контейнер сообщений по `[is-mine]` элементам
- **`findChatQuickReplies()`** — ищет `div`/`span` с `cursor: pointer` внутри контейнера чата. Стратегия 1: все cursor:pointer внутри chat container (не в `[is-mine="mine"]`). Стратегия 2: fallback внутри последнего бот-сообщения + siblings. Фильтры: размер, видимость, дедупликация вложенных

### Changed
- **`findQuickReplyButtons()`** — приоритет: `findChatQuickReplies()`, fallback: стандартные `<button>` в chat area
- **`findAllClickableWithText()`** — мержит стандартные кнопки + chat quick replies
- **`complaint_subtype` для seller** — добавлен паттерн `'плагиат'` как fallback
- **`complaint_detail` для brand** — добавлены паттерны `'фото'`, `'видео'`, `'текст'`
- **Debug output** — `chatQuickReplies` с размерами (WxH+tag), `hasChatContainer`, fix `lastMessages` → `lastBotMsg`/`lastUserMsg`
- **Размерные фильтры кнопок** — расширены с 30-450×16-70 до 20-700×14-80px (длинные кнопки типа «Использование моих фото, видео, текста» не проходили)
- **`clickQuickReply`** — 8 попыток вместо 5, подробный console.log для диагностики, встроенный fallback через `findAllClickableWithText()` на каждой попытке
- **Прокрутка лога жалоб** — popup автоматически прокручивается к последней записи лога (`scrollIntoView`)

## [5.2.0] - 2026-03-20
### Changed (CRITICAL)
- **`findSendButton()` — по SVG path** — кнопка отправки и кнопка прикрепления имеют одинаковый класс `om_17_a1`. Теперь различаются по SVG path: стрелка отправки = `M5.086`, скрепка = `M11.055`. Fallback: последняя SVG-кнопка в контейнере textarea
- **`detectPhase()` — по последнему сообщению бота** — полностью переписан. Использует атрибут `[is-mine]` для чтения истории чата (is-mine="" = бот, is-mine="mine" = пользователь). Фаза определяется по ПОСЛЕДНЕМУ сообщению бота + наличию ответа пользователя. Это позволяет боту понимать, на каком шаге остановился чат
- **`in_progress` с detail** — если последнее сообщение от пользователя (бот ещё не ответил), возвращается `in_progress` с detail: `article_sent_waiting` или `file_sent_waiting`. Service-worker корректно обновляет `item.step` из detail

### Added
- **`readChatHistory()`** — новая функция, читает все сообщения чата по `[is-mine]` атрибуту с определением отправителя и типа (текст/файл)
- **`getLastMessages()`** — возвращает последнее сообщение бота, пользователя и общее для точного определения фазы
- **Авто-продолжение с середины чата** — бот корректно определяет фазу даже если чат уже был начат ранее: если бот попросил артикул и пользователь уже отправил → `in_progress`, если бот попросил файл → `waiting_attachment`

### Fixed
- **Кнопка отправки путалась с кнопкой прикрепления** — обе имели одинаковый класс и обе содержали SVG. Бот кликал по скрепке вместо стрелки → файл не отправлялся
- **Phase detection на старых сообщениях** — ранее `readLastMessages` собирал ВСЕ видимые текстовые блоки, включая старые сообщения о «скрыли товар» от предыдущих артикулов. Теперь используется `[is-mine]` для точного чтения последнего сообщения бота

## [5.1.0] - 2026-03-19
### Changed (BREAKING)
- **Полная автономия бота** — убран confirm gate (ручное подтверждение каждого SKU). Бот сам отправляет артикулы, прикрепляет и отправляет файлы, ждёт результат, переходит к следующему
- **Режим «Пошаговый» → «Автоматический»** — в popup вместо supervised с confirm gate теперь полностью автоматический режим
- **`attachFile()` теперь ОТПРАВЛЯЕТ файл** — после прикрепления файла через DataTransfer API автоматически кликает кнопку отправки (SVG-кнопка или Enter fallback). Это исправляет бесконечный цикл `waiting_attachment`
- **Sub-state tracking** — каждый элемент очереди имеет `item.step` (`null` → `article_sent` → `file_sent` → `completed`), что предотвращает повторную отправку артикула/файла при повторном обнаружении той же фазы
- **Защита от зацикливания** — если одна и та же фаза повторяется 4 раза подряд, бот делает debug-дамп и останавливается с диагностикой
- **`has_buttons` и `unknown` фазы** — вместо остановки бот пытается найти знакомую кнопку среди доступных или ждёт обновления

### Added
- **`clickSend` action** — новое действие в content script для явного нажатия кнопки отправки
- **Расширенный `findSendButton()`** — ищет SVG-кнопку отправки в 7 уровнях родительских контейнеров + fallback по позиции (правый нижний угол)
- **`in_progress` фаза** — детекция состояния «бот обрабатывает» по ключевым словам (обрабатываю, проверяю, подождите)

### Removed
- **Confirm gate** — убран из popup.html, popup.js, service-worker.js. Больше нет `supportConfirm`, `supportSkipItem`, `awaitingConfirm`
- **Режим «Пошаговый»** — заменён на «Автоматический»

### Fixed
- **Бесконечный цикл `waiting_attachment`** — файл прикреплялся но не отправлялся (кнопка send не кликалась после DataTransfer)
- **Повторная отправка артикула** — если фаза `waiting_article` определяется повторно после отправки, бот проверяет `item.step` и ждёт смену фазы вместо повторного ввода

## [5.0.0] - 2026-03-19
### Changed (BREAKING)
- **`content/support-automation.js` v5.0.0** — полная переписка. Теперь работает в ISOLATED world (не MAIN), прямая коммуникация через `chrome.runtime.onMessage` без bridge. Поиск элементов по ТЕКСТУ и позиции viewport (правая половина = область чата), не по CSS-классам (которые OZON меняет при деплоях)
- **`background/service-worker.js`** — `injectSupportScripts()` инжектирует только `support-automation.js` (ISOLATED world). Убрана функция `pageClickGroup()` — группы теперь ищутся через `pageClickButton()` по тексту
- **Архитектура коммуникации** — убран bridge-паттерн (ISOLATED → postMessage → MAIN). DOM-события (PointerEvent, MouseEvent) работают из ISOLATED world напрямую

### Added
- **`simulateRealClick()`** — полная цепочка React 17+ событий: `pointerover → pointerenter → pointerdown → pointerup → mouseover → mouseenter → mousedown → mouseup → click → native click()`
- **`findQuickReplyButtons()`** — поиск кнопок в правой половине viewport (область чата, не сайдбар) с scoring по ключевым словам жалоб
- **`findButtonByText(patterns)`** — scoring-поиск по всем видимым кликабельным элементам на странице
- **`readLastMessages(count)`** — чтение текстовых блоков из области чата для определения фазы
- **`detectPhase()`** — определение состояния чата по тексту сообщений и кнопок (вместо CSS-классов)
- **Viewport position filtering** — все поисковые функции фильтруют элементы по позиции (правая ~70% = чат, левая ~30% = сайдбар), что устраняет ложные срабатывания на элементах списка чатов

### Removed
- **`content/support-bridge.js`** — удалён (bridge больше не нужен)
- **`pageClickGroup()`** — убрана из service-worker (заменена на `pageClickButton`)

### Fixed
- **«Фаза: item_completed, кнопки: [только новые]»** — content script находил элементы из сайдбара (список чатов) вместо области разговора. Решение: viewport-фильтрация + текстовый поиск
- **Ложные CSS-селекторы** — `.om_1_n2`, `.m9d-x7` и прочие менялись между деплоями OZON. Теперь поиск исключительно по тексту

## [4.4.0] - 2026-03-19
### Changed (BREAKING)
- **Программная инъекция content scripts** — seller.ozon.ru скрипты убраны из manifest `content_scripts`. Теперь инжектируются программно через `chrome.scripting.executeScript()` при старте жалоб. Это решает проблему «Could not establish connection» — manifest-скрипты не работают на уже открытых вкладках
- **`sendToSupport()` авто-инжект** — при ошибке связи автоматически инжектирует скрипты и повторяет запрос (не требует перезагрузки вкладки)
- **`ensureSellerChatPage()` + инжект** — после навигации на чаты всегда инжектирует скрипты (guard внутри скриптов защищает от двойной инъекции)

### Added
- **Сохранение файлов доказательств** — загруженные файлы конвертируются в base64 и сохраняются в `chrome.storage.local` ключ `complaintFilesData`. Файлы переживают закрытие popup и переиспользуются между сессиями. Удаление — только кнопкой × на файле
- **Восстановление файлов** — при открытии popup файлы автоматически восстанавливаются из storage

### Fixed
- **«Could not establish connection»** — корневая причина: manifest content_scripts не инжектируются в уже открытые вкладки. Решение: программная инъекция через `chrome.scripting.executeScript`

## [4.3.1] - 2026-03-19
### Changed (CRITICAL)
- **simulateRealClick()** — полная цепочка событий по спецификации OZON Brand Guard: `focus → pointerover → pointerenter → pointerdown → delay → pointerup → mouseover → mouseenter → mousedown → delay → mouseup → click → native click()`. React 17+ на OZON использует PointerEvent delegation — без полной цепочки клики не регистрируются
- **setInputValueSmart()** — ввод текста через native prototype setter (обход React proxy) для textarea/input, `document.execCommand('insertText')` для contenteditable
- **attachFile()** — использует `fetch(dataUrl) → blob → File` (как в reference project) вместо `atob + Uint8Array`
- **parseGroups()** — 3 стратегии поиска: CSS-классы `.m9d-x7`, поиск по role/button + текст, TreeWalker по текстовым нодам
- **parseOpenChat()** — расширенный поиск кнопок (scoring по ключевым словам чата), 5 стратегий поиска textarea (footer → чат → глобально → contenteditable → deepQuerySelector)
- **clickButton/clickGroup/clickNewChat** — retry-логика (3-5 попыток × 2с), DOM может быть не готов из-за lazy load

### Added
- **debugDOM()** — диагностический метод, возвращает полный DOM snapshot (группы, кнопки, поля ввода, URL) для отладки
- **deepQuerySelector()** — поиск через Shadow DOM и iframes (из reference)
- **waitForElement()** — утилита retry-поиска элемента с задержкой
- **getOwnText()** — извлечение только собственного текста элемента (не потомков)
- **Debug logging в no_chat** — при фазе no_chat логируется полный DOM snapshot (URL, группы, видимые кнопки)
- **Ожидание SPA** — 5с пауза после навигации перед первым взаимодействием, 8с после перехода на /app/messenger
- **DOM guard** — защита от двойной инъекции через DOM-элемент `#__ozguard-support-guard` (как в reference)

### Fixed
- **Бесконечный цикл no_chat** — добавлена повторная проверка состояния после 5с ожидания, debug-вывод для диагностики
- **SPA загрузка** — увеличено ожидание `ensureSellerChatPage` с 4с до 8с (React SPA OZON грузится долго)
- **Кнопка отправки не найдена** — fallback через Enter keydown если sendButton не обнаружен

## [4.3.0] - 2026-03-19
### Changed (BREAKING)
- **Жалобы: supervised automation** — полная переработка модуля жалоб. Режим «Полное авто» заменён на «Пошаговый» (supervised): бот автоматически навигирует по дереву чата (Поддержка → Личный кабинет → Кабинет бренда → Жалоба → Плагиат), но перед отправкой каждого артикула показывает **confirm gate** — кнопку подтверждения в popup
- **Confirm gate UI** — блок с кнопками «Отправить», «Пропустить», «Стоп» появляется перед каждой отправкой артикула. Показывает номер SKU, индекс в очереди, количество файлов
- **Неожиданные фазы = стоп** — при любом неизвестном состоянии DOM бот останавливается вместо попытки продолжить (безопасность)

### Added
- **Реалистичная эмуляция кликов** — `safeClick()` использует полную цепочку: `pointerdown → mousedown → delay → pointerup → mouseup → click` с координатами внутри элемента и случайным смещением
- **Посимвольный ввод** — `safeTypeText()` вводит текст по одному символу с `keydown/keyup` событиями и случайными задержками 30-90мс между символами
- **Восстановление confirm gate** — при переоткрытии popup, если бот ждёт подтверждения, confirm gate восстанавливается из `supportGetStatus`

### Fixed
- **Антибот при циклах** — убраны избыточные долгие паузы для маленьких очередей (< 20 SKU)

### Removed
- **Режим «Полное авто»** — заменён на supervised «Пошаговый» для безопасности

## [4.2.1] - 2026-03-19
### Changed (BREAKING)
- **Архитектура жалоб: bridge-паттерн** — MAIN world не имеет `chrome.runtime`, а `chrome.scripting.executeScript` блокируется. Решение: `support-bridge.js` (ISOLATED world, имеет `chrome.runtime.onMessage`) пересылает запросы в `support-automation.js` (MAIN world) через `window.postMessage`
- **content/support-automation.js** — библиотека на `window.__ozguard_support` + слушатель `window.postMessage` для приёма запросов от bridge
- **content/support-bridge.js** — новый файл, ISOLATED world, `chrome.runtime.onMessage` → `window.postMessage` → ответ обратно
- **service-worker.js** — `executeInPage()` → `sendToSupport()` через `chrome.tabs.sendMessage` к bridge
- **Путь жалобы (brand)** — по реальному DOM Ozon: Личный кабинет → Кабинет бренда → Жалоба на товар/продавца → Плагиат карточек товара → Использование моих фото, видео, текста
- **Цикл «Пожаловаться на другой товар»** — без нового чата, как в реальном боте
- **Фазы чата** — `direction_selection`, `category_selection`, `complaint_type`, `complaint_subtype`, `complaint_detail`, `waiting_article`, `waiting_attachment`, `item_completed`, `ready_for_next`
- **Селекторы DOM обновлены** — из реального HTML: `.m9d-x7`, `.m9d-y`, `.m9d-c0`, `.om_1_n6`, `.om_31_a`, `.om_1_e2`, `.s3c110-a`

### Added
- **Автопереход на чаты** — `/app/messenger/?group=support_v2`
- **Сохранение/восстановление сессии жалоб** — SKU, режим, тип, логи в `chrome.storage.local`
- **Восстановление прогресса** — popup запрашивает статус из service worker при открытии
- **Кнопка «Очистить»** — очистка сессии жалоб
- **Конвертация файлов в base64** — `filesToBase64()` в popup.js

### Fixed
- **«Receiving end does not exist»** — MAIN world `chrome.runtime` недоступен → bridge
- **«Cannot access contents of the page»** — `chrome.scripting.executeScript` блокируется → `chrome.tabs.sendMessage` + bridge

## [4.1.0] - 2026-03-19
### Added
- **Таб «Жалобы» (PRO)** — 4-й таб в popup для автоматизации подачи жалоб через чат поддержки seller.ozon.ru
- **2 режима работы** — Тестовый (dry run, только лог без действий) и Полное авто (автоматически выполняет все шаги в чате)
- **Кнопка «В жалобы»** — в разделе Сканирование, собирает SKU конкурентов из результатов и отправляет в таб Жалобы
- **Content script `support-automation.js`** — парсер DOM seller.ozon.ru (группы чатов, список чатов, сообщения, quick-reply кнопки), state machine (~20 состояний), безопасные действия (safeClick, safeTypeText, safeAttachFile)
- **Service worker handlers** — маршрутизация сообщений, проверка PRO-лицензии, очередь жалоб, сохранение сессий
- **Очередь жалоб** — визуальная очередь SKU со статусами (pending/done/failed/skipped), прогресс-бар
- **Прикрепление файлов** — drag-n-drop зона для доказательств (скриншоты, PDF)
- **2 типа жалоб** — «Нарушение правил продавцом» и «Жалоба от бренда»
- **Предупреждения** — при 20+ и 50+ SKU предупреждение об оценке времени
- **Антибот** — задержки 5-8 сек между жалобами, доп. пауза каждые 10/30 жалоб, humanDelay ±30% джиттер

### Fixed
- **Лицензия `maxlength`** — поле ввода было 19 символов вместо 21 (формат OZG-XXXXX-XXXXX-XXXXX = 21)
- **Автоформат лицензии** — исправлена логика автовставки дефисов при вводе кода

### Changed
- **manifest.json** — добавлен `seller.ozon.ru` в `host_permissions` и `content_scripts`, версия 4.1.0
- **popup.html** — 4 таба вместо 3, версия v4.1, кнопка «В жалобы» в export-actions
- **popup.js** — обработка сообщений support automation, UI управления жалобами, кнопка передачи SKU в жалобы, предупреждения по количеству
- **popup.css** — стили для таба жалоб (опции, файлы, статус, очередь, лог)

## [4.0.0] - 2026-03-18
### Added
- **Paywall FREE/PRO** — система монетизации: одиночная проверка SKU и экспорт бесплатно, множественная проверка (2+ SKU), пакетный XLSX импорт и будущие функции — под PRO-лицензией
- **Система лицензий** — формат `OZG-XXXXX-XXXXX-XXXXX`, оффлайн-валидация, хранение в `chrome.storage.local`
- **UI лицензии** — секция «Лицензия PRO» в Настройках: ввод кода, автоформат, активация/деактивация
- **PRO/FREE бейдж** — в хедере расширения отображается текущий статус
- **Двойная проверка** — paywall проверяется и в popup.js (UI) и в service-worker.js (backend)
- **Визуальная блокировка** — batch-зона показывает замок «🔒 PRO» для FREE-пользователей

### Changed
- **service-worker.js** — добавлены функции `validateLicenseCode`, `getLicenseStatus`, `activateLicense`, `deactivateLicense`, обработчики сообщений лицензии, проверка лицензии при startScan
- **popup.js** — загрузка статуса лицензии при инициализации, paywall-проверки перед множественным сканированием и batch-импортом
- **project.md** — обновлена архитектура с описанием paywall-системы

## [3.7.0] - 2026-03-18
### Added
- **Пакетный сбор** — загрузка XLSX шаблона «Цены товаров» из OZON, извлечение числовых SKU из колонки B (строки 4+), запуск массового сканирования
- **XLSX парсер** — встроенный минимальный ZIP+XML парсер без внешних зависимостей, поддержка sharedStrings и deflate
- **Кнопка «Только SKU»** — копирует только SKU конкурентов (уникальные) в буфер обмена
- **Копирование SKU в модалке** — мини-кнопка `⎘` рядом с каждым SKU конкурента в модальном окне истории
- **Предупреждения для больших пакетов** — при 100+ и 1000+ SKU показывает рекомендации по задержке и оценку времени
- **Антибот для пакетов** — дополнительная пауза 10-20 сек каждые 20 SKU при сканировании 50+ товаров

### Fixed
- **Кнопка копирования SKU** — выровнена по базовой линии цифр (inline-flex + baseline)
- **Модальное окно** — исправлена верстка: overflow hidden на контейнере, min-height для body

## [3.6.0] - 2026-03-18
### Fixed
- **Парсер цен** — глубокий парсер: `JSON.stringify(item.price)` → regex `\d+\s*₽`, fallback по всем вложенным полям (price, cardPrice, originalPrice, value, amount, text)
- **Excel предупреждение** — вернул CSV с BOM+`;` (Excel открывает без предупреждений), расширение `.csv`
- **Иконка** — обновлена по дизайну logo.svg (синий градиент со щитом)

### Added
- **Сохранение сессии** — последние результаты + логи сохраняются в `chrome.storage.local`; при повторном открытии popup восстанавливаются автоматически
- **Копирование SKU** — мини-кнопка `⎘` рядом с каждым артикулом конкурента, копирует SKU в буфер обмена
- **Диагностика цен** — лог структуры первого seller + raw JSON price для отладки

### Changed
- **Светлая тема** — белый фон, серые бордеры, синие акценты
- **Лог авто-раскрыт** — секция лога по умолчанию открыта
- **Антибот** — `simulateHumanBehavior()` + `humanDelay()` ±30% джиттер на всех паузах

## [3.3.0] - 2026-03-18
### Fixed
- **Цены** — `price` от OZON — объект `{price: "1 590 ₽"}`, а не строка; исправлен парсер
- **SKU кликабельны** — артикул конкурента теперь ссылка на карточку товара
- **Чистые логи** — убрана диагностика, оставлены только ключевые этапы

### Changed
- **Modal endpoint** — `/modal/otherOffersFromSellers?product_id=...` вместо `/sellers/` (возвращал ту же страницу)
- **Парсер `webSellerList`** — точный парсер под формат: `sellers[].{sku, id, name, link, price, productLink}`
- **UI** — SKU конкурента → ссылка на `ozon.ru/product/{sku}/`, продавец → ссылка на магазин

## [3.2.0] - 2026-03-17
### Changed
- **Двухшаговый парсинг** — главная страница товара → извлечение `count` из `webBestSeller` → запрос modal для получения полного списка продавцов

## [3.1.0] - 2026-03-17
### Fixed (CRITICAL)
- **SKU ≠ Product ID** — `/product/{sku}/` редиректил на поиск (SKU — это артикул, не ID товара в URL). Теперь: поиск SKU на OZON → клик по первому результату → извлечение данных со страницы товара
- **2 виджета вместо 30+** — фоновая вкладка (`active: false`) получала минимальный контент. Теперь: отдельное окно (`chrome.windows.create`)
- **Interceptor перезаписывал данные** — OZON грузит виджеты несколькими API-запросами, interceptor ловил только первый. Теперь: `Object.assign` мержит все `widgetStates`

### Changed
- **Поток сканирования** — search → find product → navigate → scroll → intercept → parse
- **Рабочее окно** — `chrome.windows.create({ focused: false })` вместо фоновой вкладки
- **Агрессивный скролл** — 3 прохода страницы с паузами для триггера lazy-load
- **Polling** — ждёт стабилизации `widgetCount` (3 секунды без изменений)
- **Диагностика** — лог показывает все имена виджетов, URL API-запросов, DOM-статистику

## [3.0.1] - 2026-03-17
### Fixed
- **Тест подключения** — убрана навигация на товар; просто проверяет открытую вкладку ozon.ru
- **Рабочая вкладка** — отдельная фоновая вкладка, не трогает пользовательские
- **Cleanup** — фоновая вкладка автоматически закрывается после завершения

## [3.0.0] - 2026-03-17
### Changed (BREAKING)
- **Архитектура** — полностью новый подход: вместо прямых API-вызовов (403) расширение **навигирует вкладку** на страницу товара и перехватывает данные, которые OZON сам загружает
- **background/service-worker.js** — `fetchProductData` теперь: навигация → ожидание → чтение перехваченных данных → DOM fallback
- **Скорость** — ~10 сек/SKU (медленнее, но работает без блокировок)

### Added
- **content/interceptor.js** — content script в `world: MAIN`, перехватывает `fetch`/`XHR` к API OZON при загрузке страницы
- **DOM extraction** — fallback парсинг: ищет `__NEXT_DATA__`, JSON в `<script>` тегах, ссылки на `/seller/` в DOM
- **manifest.json** — `content_scripts` с `run_at: document_start` для перехвата до загрузки страницы

### Fixed
- **403 блокировка** — больше нет прямых API-вызовов, данные берутся из штатной загрузки страницы OZON

## [2.1.0] - 2026-03-17
### Fixed (CRITICAL)
- **403 блокировка** — прямые fetch из service-worker блокировались OZON (отсутствие cookies, подозрительный UA). Теперь API-запросы выполняются через `chrome.scripting.executeScript` **из контекста вкладки ozon.ru** (same-origin, с cookies, выглядит как обычный XHR)
- **Бан IP** — убран заголовок `User-Agent: ozonapp_android`, который тригерил блокировку

### Changed
- **Архитектура** — гибридный подход: одна фоновая вкладка ozon.ru + fetch API из её контекста
- **manifest.json** — возвращены `scripting`, `tabs` (нужны для executeScript в рабочую вкладку)
- **host_permissions** — убран `api.ozon.ru` (запросы идут same-origin через вкладку)

### Added
- **Настройки → Инструкция** — блок "Как это работает" с требованиями (авторизация, не закрывать вкладку)
- **Автоиспользование существующей вкладки** — если ozon.ru уже открыт, расширение использует его
- **Настройка задержки** — по умолчанию 2000мс, рекомендации по значениям

### Removed
- **Поле API-ключа** — не требуется, запросы идут через браузер пользователя

## [2.0.0] - 2026-03-17
### Changed (BREAKING)
- **Архитектура** — полностью переписан парсер: вместо открытия вкладок + DOM-парсинг теперь используется прямой запрос к внутреннему API OZON (`composer-api.bx/page/json/v2` и `entrypoint-api.bx/page/json/v2`)
- **background/service-worker.js** — fetch к API с retry, fallback между эндпоинтами, rate-limit обработка
- **manifest.json** — убраны permissions `activeTab/scripting/tabs`, добавлен host `api.ozon.ru`
- **Скорость** — сканирование в 10x быстрее (1.5с между SKU вместо 5-8с с вкладками)

### Added
- **Таб "Настройки"** — проверка подключения к API, поле для своего API-ключа, настройка задержки между запросами
- **Диагностика** — тест API показывает список виджетов, доступных на странице товара
- **Парсинг** — 3 метода извлечения продавцов: `sellerList` виджет, `webSale/OtherSeller` виджеты, глубокий поиск по всем `widgetStates`
- **Логирование** — подробный лог с именами найденных виджетов и ключами ответа API

### Removed
- **content/content-scan.js** — удалён (DOM-парсинг больше не используется)
- **permissions** — `activeTab`, `scripting`, `tabs` больше не нужны

## [1.1.0] - 2026-03-17
### Fixed
- content-scan.js — ISOLATED world, скролл для lazy-load, retry

### Added
- Табы Сканирование/История, история до 10 сессий, модалка с деталями, CSV из истории

## [1.0.0] - 2026-03-17
### Added
- Первый релиз: popup UI, service-worker, content-scan.js, placeholder иконки
